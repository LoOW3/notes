En java, **enum** es una palbra clave utilizada para definir un tipo de dato especial que representa un **conjunto fijo de constantes**.

Estas constantes son valores predefinidos que representan elementos distintos de un conjunto.

```java
enum DiaSemana {
	LUNES, MARTES, MIERCOLES, JUEVES, VIERNES, SABADO, DOMINGO
}
```

```java

package com.mycompany.enums;

public class Enums {
    enum Color {
        ROJO, AZUL,VERDE, AMARILLO
    };
    public static void main(String[] args) {
        Color color = Color.ROJO;
        System.out.println("color" + color);
        
        
        //metodo values para obtener los valores de enum
        for(Color c : Color.values()){
            System.out.println(c);
        }
    }
}

```

## metodos de enum

- **name()**: Devuelve un String con el nombre de la constante
- **ordinal()**: Devuelve un entero con la posicion del enum segun esta declarada
- **values()**: Devuelve un array que contiene todos los elementos del enum
- **valueOf(String name)**: Se utiliza para obtener la constante del enum cuyo nombre coincide con la cadena proporcionada.

## Extra

Es buena idea crear los enums en un package distinto

Para ello en netbeans sobre el proyecto
New > Other > Java Enum

```java

package enums;

public enum Color {
    ROJO, AZUL,VERDE, AMARILLO;
}

```

```java

package com.mycompany.enums;

import enums.Color;

public class Enums {
    public static void main(String[] args) {
        for(Color color: Color.values()){
            System.out.println(color);
        }
        
        Color col = Color.AZUL;
        System.out.println(col.name());
        System.out.println(col.ordinal());
        
        
        col = Color.valueOf("AZUL");
        
    }
}

```
